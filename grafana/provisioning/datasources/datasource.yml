apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    # 👇 QUAN TRỌNG: Phải set UID này giống hệt trong file JSON của bạn
    uid: df6nugk7yzqpsc
    jsonData:
      timeInterval: "5s"